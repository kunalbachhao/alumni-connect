# .env

# Secret for JWT token generation
JWT_SECRET=Pass@123

# Secret for session management
SESSION_SECRET=Pass@123

# MongoDB connection string (if you're using MongoDB with Mongoose)
MONGODB_URI=mongodb://127.0.0.1:27017/DBMS

# Port (optional, defaults to 3000)
PORT=3000
