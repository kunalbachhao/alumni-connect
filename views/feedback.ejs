<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Session Feedback</title>
    <!-- Use the Inter font from Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <!-- Load Tailwind CSS from CDN for utility-first styling -->
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/stylesheets/feedback.css">
    
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen p-6">

    <!-- The Feedback Form -->
    <div class="bg-white p-8 rounded-2xl shadow-2xl w-full max-w-md">
        <div class="text-center mb-6">
            <h2 class="text-3xl font-extrabold text-gray-800 mb-1">How Was Your Session?</h2>
            <p class="text-md text-gray-500">Please share your thoughts on the session.</p>
        </div>

        <form id="feedbackForm" class="space-y-5">
            <div class="bg-blue-50 p-5 rounded-xl border border-blue-100">
                <p class="text-center text-gray-700 text-sm font-medium mb-3">
                    Rate your session with <span id="mentorNamePlaceholder" class="font-bold text-blue-600">your mentor</span>.
                </p>
                
                <div class="star-rating" id="starRating">
                    <span class="star" data-value="5" data-label="Excellent">&#9733;</span>
                    <span class="star" data-value="4" data-label="Great">&#9733;</span>
                    <span class="star" data-value="3" data-label="Good">&#9733;</span>
                    <span class="star" data-value="2" data-label="Poor">&#9733;</span>
                    <span class="star" data-value="1" data-label="Terrible">&#9733;</span>
                </div>
                <p id="ratingText" class="text-center text-xs text-gray-500 mt-2"></p>
            </div>

            <div>
                <label for="feedback" class="block text-sm font-semibold text-gray-700 mb-2">Optional Feedback</label>
                <textarea id="feedback" name="feedback" rows="3" class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-colors duration-200 resize-none" placeholder="Share any additional comments or suggestions..."></textarea>
            </div>

            <button type="submit" id="submitBtn" class="w-full bg-blue-600 text-white py-2.5 rounded-lg font-bold text-md hover:bg-blue-700 transition-colors duration-300 focus:outline-none focus:ring-4 focus:ring-blue-500 focus:ring-opacity-50">Submit Feedback</button>
        </form>
    </div>

    <!-- The Success Pop-up Modal -->
    <div id="successModal" class="modal-overlay hidden">
        <div class="modal-content bg-white p-10 rounded-2xl shadow-2xl w-full max-w-sm text-center">
            <p class="text-xl font-bold text-gray-800 mb-2">You've done it! 🎉</p>
            <p class="text-2xl text-green-600 font-bold">Thank You for Your Feedback!</p>
            <p class="text-sm text-gray-500 mt-2">Your feedback has been successfully submitted and helps us improve our mentorship program.</p>
        </div>
    </div>

    <!-- Confetti canvas for celebratory effect -->
    <canvas id="confettiCanvas" class="confetti"></canvas>

    <!-- Custom message box UI for alerts/confirms -->
    <div id="messageBox" class="custom-message-box hidden">
        <p id="messageText" class="mb-4 text-gray-700"></p>
        <div class="flex justify-center gap-4">
            <button id="okBtn" class="px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700">OK</button>
            <button id="cancelBtn" class="hidden px-4 py-2 bg-gray-300 text-gray-800 rounded-lg hover:bg-gray-400">Cancel</button>
        </div>
    </div>
    <script src="/javascripts/feedback.js"></script>
</body>
</html>